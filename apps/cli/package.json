{
	"name": "pdfa-convert-cli",
	"version": "0.1.0",
	"description": "CLI for converting PDFs to PDF/A",
	"license": "WTFPL",
	"type": "module",
	"bin": {
		"pdfa-convert": "./dist/cli.js"
	},
	"files": [
		"dist"
	],
	"scripts": {
		"prebuild": "rimraf dist",
		"build": "tsc --build tsconfig.build.json",
		"test": "vitest"
	},
	"dependencies": {
		"pdfa-convert": "workspace:*"
	},
	"devDependencies": {
		"rimraf": "^6.1.3",
		"typescript": "^5.0.0",
		"vitest": "^1.0.0"
	},
	"engines": {
		"node": ">=18"
	}
}
