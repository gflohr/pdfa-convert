# Glyph Outlines & Path Operations

<!--
TODO!
Looking up a glyph by character or ID (font.glyphForCodePoint()), converting
glyph paths to SVG path strings (glyph.path.toSVG()), and drawing paths onto
HTML5 <canvas>/Node.js canvas contexts.
-->
